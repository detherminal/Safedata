<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Safedata" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/mint_background</item>
        <item name="colorPrimaryVariant">@color/mint_background</item>
        <item name="colorOnPrimary">@color/mint_background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/mint_background</item>
        <item name="colorSecondaryVariant">@color/mint_background</item>
        <item name="colorOnSecondary">@color/mint_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>